# Bot principal (já usados pelo bot atual)
numpy
pandas
python-dotenv
eth-account
requests

# Persistência online (modo supabase)
supabase>=2.0.0

# Criptografia (para SaaS multiusuário)
cryptography>=41.0.0